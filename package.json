{
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "name": "core-radar",
  "version": "1.0.0",
  "description": "A weather radar and alert viewer",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=public/app.js --minify --sourcemap",
    "watch": "esbuild src/index.ts --bundle --outfile=public/app.js --minify --sourcemap --watch"
  },
  "author": "mbg206",
  "license": "ISC",
  "dependencies": {
    "maplibre-gl": "^5.24.0"
  }
}
